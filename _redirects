# Path rules only. Workers Static Assets rejects an absolute URL here —
# "Only relative URLs are allowed" — so the docs.postboi.email → docs.postboi.app
# forward cannot live in this file; it is a zone Redirect Rule on postboi.email.
# (Absolute sources work on Pages, not on Workers. Different product, same filename.)

# The Cloud → provider rename: keep old bookmarks working.
/cloud /provider 301

# Tempboi is the product's name; the page kept its original slug.
/tempboi /temp-inbox 301
/raw/tempboi /raw/temp-inbox 301
