/**
 * Placeholder for the generated `from` types. On Postboi Cloud, `bunx postboi init` /
 * `bunx postboi sync` overwrite this module's compiled `.d.ts` inside node_modules with a
 * `Register` augmentation narrowing `from` to the account's addresses — no project file,
 * nothing to commit. Untouched (or after a reinstall), `Register` stays empty and `from`
 * falls back to any `Email`, so builds never depend on the generated types existing.
 */
export {}
